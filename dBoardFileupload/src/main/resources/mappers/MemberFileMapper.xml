<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.example.dao.MemberFileDAO">

	<insert id="insertMemberFile" parameterType="MemberFileVO">
		INSERT INTO
			member_file(member_id, originFilename, filename, filepath)
		VALUES
			(#{member_id},#{originFilename},#{filename},#{filepath})
	</insert>

	
	

</mapper>